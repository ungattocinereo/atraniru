# Тестирование контактной формы

## Быстрый тест через браузер

1. Откройте: **http://localhost:4322/contacts**
2. Заполните все поля:
   - Имя: `Тестовый Пользователь`
   - Email: `test@example.com`
   - Сообщение: `Это тестовое сообщение с сайта Atrani.ru`
3. Нажмите **"Отправить сообщение"**
4. Проверьте:
   - ✅ Форма превратилась в конверт с анимацией
   - ✅ Появилась галочка в правом верхнем углу
   - ✅ Показано сообщение благодарности
   - ✅ Есть кнопка "Отправить ещё сообщение"
5. Откройте Telegram канал **"Amalfi.Day [Tech]"** и проверьте сообщение

## Тест через curl

### Простой тест

```bash
curl -X POST http://localhost:4322/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовый Пользователь",
    "email": "test@example.com",
    "message": "Тестовое сообщение через API"
  }'
```

Ожидаемый результат:
```json
{"success":true,"message":"Сообщение успешно отправлено"}
```

### Тест с кириллицей

```bash
curl -X POST http://localhost:4322/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Григорий Монтегриш",
    "email": "greg@atrani.ru",
    "message": "Привет! Хочу забронировать апартаменты на июль. Есть ли свободные номера с видом на море?"
  }'
```

### Тест с HTML (проверка escape)

```bash
curl -X POST http://localhost:4322/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test <script>alert(1)</script>",
    "email": "test@example.com",
    "message": "Message with <b>HTML</b> tags"
  }'
```

В Telegram должны отобразиться экранированные символы (безопасно).

### Тест без обязательных полей (должна быть ошибка)

```bash
curl -X POST http://localhost:4322/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test",
    "email": ""
  }'
```

Ожидаемый результат:
```json
{"success":false,"error":"Все поля обязательны для заполнения"}
```

## Проверка в Telegram

После отправки сообщения проверьте канал **"Amalfi.Day [Tech]"** (ID: -1002455823047).

Должно прийти сообщение в формате:

```
🗼 Сообщение с сайта Atrani.ru

👤 Имя: Тестовый Пользователь
📧 Email: test@example.com

💬 Сообщение:
Тестовое сообщение через API

━━━━━━━━━━━━━━━━━━━━
📊 Технические данные:

🌐 IP-адрес: [ваш IP]
🕐 Дата/Время: [текущая дата]
🖥 User Agent: [ваш браузер]
🔗 Referer: [URL]
🌍 Язык: [язык]
```

## Troubleshooting

### Ошибка: "Telegram не настроен"
- Проверьте `.env` файл
- Убедитесь что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` заполнены

### Ошибка: "Ошибка отправки в Telegram"
- Проверьте что бот добавлен в канал
- Проверьте что бот имеет права на отправку сообщений
- Проверьте токен бота в @BotFather

### Не приходят сообщения в Telegram
- Проверьте Chat ID (должен начинаться с `-`)
- Проверьте что бот администратор в канале
- Проверьте логи в терминале dev server

### Форма не отправляется
- Откройте DevTools → Console
- Проверьте сетевые запросы в Network tab
- Убедитесь что все поля заполнены

## Production deployment

Перед деплоем на production:

1. ✅ Создайте `.env` на сервере с правильными токенами
2. ✅ Настройте HTTPS
3. ✅ Проверьте права бота в канале
4. ✅ Добавьте rate limiting (опционально)
5. ✅ Настройте мониторинг ошибок
