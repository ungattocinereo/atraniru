---
// BlogPreview component - 4 latest posts from Ghost
import { ghostClient, rewriteGhostUrl } from '../lib/ghost';

let posts = [];
let error = null;

try {
  posts = await ghostClient.posts.browse({
    limit: 8,
    include: 'tags,authors'
  });
} catch (e) {
  error = e;
  console.warn('Ghost API error:', e);
  // Fallback: show placeholder posts
  posts = [
    {
      slug: 'placeholder-1',
      title: 'Скоро здесь появятся статьи',
      excerpt: 'Следите за обновлениями блога',
      feature_image: '/images/placeholder-blog-1.jpg',
      published_at: new Date().toISOString()
    },
    {
      slug: 'placeholder-2',
      title: 'Истории из Атрани',
      excerpt: 'Секреты и советы от местных жителей',
      feature_image: '/images/placeholder-blog-2.jpg',
      published_at: new Date().toISOString()
    },
    {
      slug: 'placeholder-3',
      title: 'Гастрономические открытия',
      excerpt: 'Лучшие рестораны Амальфитанского побережья',
      feature_image: '/images/placeholder-blog-3.jpg',
      published_at: new Date().toISOString()
    },
    {
      slug: 'placeholder-4',
      title: 'Путеводитель по побережью',
      excerpt: 'Всё, что нужно знать перед поездкой',
      feature_image: '/images/placeholder-blog-4.jpg',
      published_at: new Date().toISOString()
    },
    {
      slug: 'placeholder-5',
      title: 'Транспорт на побережье',
      excerpt: 'Как добраться и перемещаться по Амальфи',
      feature_image: '/images/placeholder-blog-5.jpg',
      published_at: new Date().toISOString()
    },
    {
      slug: 'placeholder-6',
      title: 'Пляжи Амальфи',
      excerpt: 'Лучшие пляжи для отдыха на побережье',
      feature_image: '/images/placeholder-blog-6.jpg',
      published_at: new Date().toISOString()
    },
    {
      slug: 'placeholder-7',
      title: 'Винные туры',
      excerpt: 'Дегустации и винодельни региона Кампания',
      feature_image: '/images/placeholder-blog-7.jpg',
      published_at: new Date().toISOString()
    },
    {
      slug: 'placeholder-8',
      title: 'Фестивали и праздники',
      excerpt: 'Традиционные события Амальфитанского побережья',
      feature_image: '/images/placeholder-blog-8.jpg',
      published_at: new Date().toISOString()
    }
  ];
}
---

<section class="blog">
  <div class="container">
    <div class="section-header reveal">
      <p class="section-label">04 — Блог</p>
      <h2 class="section-title">Истории и советы</h2>
    </div>
    <div class="blog-grid">
      {posts.map((post) => (
        <article class="blog-card reveal">
          <a href={`/blog/${post.slug}`}>
            <div class="blog-image">
              <img
                src={rewriteGhostUrl(post.feature_image) || '/images/placeholder-blog.jpg'}
                alt={post.title}
                loading="lazy"
              />
            </div>
            <div class="blog-content">
              <h3>{post.title}</h3>
              <p>{post.excerpt}</p>
              <span class="blog-date">
                {new Date(post.published_at).toLocaleDateString('ru-RU', {
                  year: 'numeric',
                  month: 'long',
                  day: 'numeric'
                })}
              </span>
            </div>
          </a>
        </article>
      ))}
    </div>
    <div class="blog-cta reveal">
      <a href="/blog" class="btn btn-primary">Все статьи</a>
    </div>
  </div>
</section>

<style>
  .blog {
    padding: 120px 0;
    background: var(--color-bg-light);
  }

  .blog-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-bottom: 60px;
  }

  .blog-card {
    background: white;
    overflow: hidden;
    transition: transform 0.3s var(--ease-out);
  }

  .blog-card:hover {
    transform: translateY(-6px);
  }

  .blog-card a {
    display: block;
    text-decoration: none;
  }

  .blog-image {
    position: relative;
    width: 100%;
    aspect-ratio: 3 / 2;
    overflow: hidden;
    background: var(--color-navy);
  }

  .blog-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s var(--ease-out);
  }

  .blog-card:hover .blog-image img {
    transform: scale(1.05);
  }

  .blog-content {
    padding: 16px 20px 20px;
  }

  .blog-content h3 {
    font-family: var(--font-display);
    font-size: 15px;
    font-weight: 700;
    color: var(--color-navy);
    margin-bottom: 8px;
    letter-spacing: -0.3px;
    line-height: 1.3;
  }

  .blog-content p {
    font-size: 13px;
    color: var(--color-gray-dark);
    line-height: 1.5;
    margin-bottom: 10px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .blog-date {
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-gray);
  }

  .blog-cta {
    text-align: center;
  }

  @media (max-width: 1024px) {
    .blog-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }
  }

  @media (max-width: 768px) {
    .blog {
      padding: 80px 0;
    }

    .blog-grid {
      grid-template-columns: 1fr;
      gap: 16px;
    }

    .blog-content {
      padding: 16px;
    }

    .blog-content h3 {
      font-size: 16px;
    }

    .blog-content p {
      font-size: 14px;
    }
  }
</style>
